node_modules/
dist/
*.tsbuildinfo

# Plan-mode scratch output. Plans that matter are committed under
# docs/superpowers/plans/ with a meaningful name.
.claude/plans/

# Runtime output from the legacy Python watcher
codex-turn-usage/
.codex-turn-watch-locks/

# Runtime output from the TypeScript CLI. Locks now live in ~/.turnscope/locks/;
# .turnscope-locks/ stays listed so leftovers from before that move are ignored.
turnscope-usage/
.turnscope-locks/
